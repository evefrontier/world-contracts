#!/usr/bin/env bash
# Runs fast checks before every commit. Skip with `git commit --no-verify`.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

echo "[pre-commit] format check"
pnpm fmt:check

echo "[pre-commit] lint"
pnpm lint

echo "[pre-commit] build"
pnpm build
